services:
  web:
    image: node:22-slim
    working_dir: /app
    command: node server.js
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
